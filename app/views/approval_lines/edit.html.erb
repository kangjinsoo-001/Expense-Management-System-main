<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
  <div class="mb-6">
    <div class="flex items-center text-sm text-gray-500 mb-4">
      <%= link_to "← 결재선 상세", approval_line_path(@approval_line), class: "hover:text-gray-700" %>
    </div>
    
    <h1 class="text-2xl font-semibold text-gray-900">결재선 수정</h1>
    <p class="mt-2 text-sm text-gray-600">
      결재선 정보와 승인 단계를 수정합니다.
    </p>
  </div>

  <%= render 'form', approval_line: @approval_line %>
</div>