<%= turbo_frame_tag "expense_form" do %>
  <div class="max-w-4xl mx-auto">
    <div class="bg-green-50 border border-green-200 rounded-lg p-6">
      <div class="flex items-center gap-3 mb-4">
        <svg class="h-6 w-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <h2 class="text-lg font-semibold text-green-900">경비 항목이 저장되었습니다</h2>
      </div>
      
      <div class="space-y-3">
        <p class="text-green-700">
          <%= expense_sheet.year %>년 <%= expense_sheet.month %>월 경비 시트에 항목이 추가되었습니다.
        </p>
        
        <div class="flex gap-3">
          <%= link_to "새 항목 추가", 
                      new_expense_sheet_expense_item_path(expense_sheet),
                      class: "inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors",
                      data: { turbo_frame: "expense_form" } %>
          
          <%= link_to "경비 시트로 돌아가기", 
                      expense_sheet_path(target_sheet || expense_sheet),
                      class: "inline-flex items-center px-4 py-2 bg-gray-600 text-white rounded-md hover:bg-gray-700 transition-colors" %>
        </div>
      </div>
    </div>
  </div>
<% end %>