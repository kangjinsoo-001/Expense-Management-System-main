<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-2xl font-semibold text-gray-900">회의실 카테고리 관리</h1>
      <p class="mt-2 text-sm text-gray-700">회의실 카테고리를 관리합니다.</p>
    </div>
    <div class="mt-4 sm:mt-0 sm:ml-16 sm:flex-none">
      <%= link_to new_admin_room_category_path, class: "inline-flex items-center justify-center rounded-md border border-transparent bg-indigo-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 sm:w-auto" do %>
        <svg class="-ml-1 mr-2 h-5 w-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
          <path d="M10.75 4.75a.75.75 0 00-1.5 0v4.5h-4.5a.75.75 0 000 1.5h4.5v4.5a.75.75 0 001.5 0v-4.5h4.5a.75.75 0 000-1.5h-4.5v-4.5z" />
        </svg>
        카테고리 추가
      <% end %>
    </div>
  </div>

  <div class="mt-8 flex flex-col">
    <div class="-my-2 -mx-4 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle md:px-6 lg:px-8">
        <div class="overflow-hidden shadow ring-1 ring-black ring-opacity-5 md:rounded-lg">
          <table class="min-w-full divide-y divide-gray-300">
            <thead class="bg-gray-50">
              <tr>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">순서</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">카테고리명</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">설명</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">회의실 수</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">상태</th>
                <th scope="col" class="relative py-3.5 pl-3 pr-4 sm:pr-6">
                  <span class="sr-only">작업</span>
                </th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200 bg-white">
              <% @room_categories.each do |category| %>
                <tr>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                    <%= category.display_order %>
                  </td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm">
                    <div class="font-medium text-gray-900"><%= category.name %></div>
                  </td>
                  <td class="px-3 py-4 text-sm text-gray-500">
                    <%= category.description.presence || "-" %>
                  </td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                      <%= category.active_rooms_count %>개
                    </span>
                  </td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm">
                    <% if category.is_active %>
                      <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                        활성
                      </span>
                    <% else %>
                      <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                        비활성
                      </span>
                    <% end %>
                  </td>
                  <td class="relative whitespace-nowrap py-4 pl-3 pr-4 text-right text-sm font-medium sm:pr-6">
                    <div class="flex justify-end gap-2">
                      <%= link_to edit_admin_room_category_path(category), class: "text-indigo-600 hover:text-indigo-900" do %>
                        수정
                      <% end %>
                      <%= button_to toggle_active_admin_room_category_path(category), method: :patch, 
                          class: "text-blue-600 hover:text-blue-900 ml-2" do %>
                        <%= category.is_active ? "비활성화" : "활성화" %>
                      <% end %>
                      <% if category.rooms.empty? %>
                        <%= button_to admin_room_category_path(category), method: :delete, 
                            data: { turbo_confirm: "이 카테고리를 삭제하시겠습니까?" },
                            class: "text-red-600 hover:text-red-900 ml-2" do %>
                          삭제
                        <% end %>
                      <% end %>
                    </div>
                  </td>
                </tr>
              <% end %>
              <% if @room_categories.empty? %>
                <tr>
                  <td colspan="6" class="px-3 py-8 text-sm text-gray-500 text-center">
                    등록된 카테고리가 없습니다.
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>