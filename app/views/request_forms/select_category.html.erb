<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900">신청서 작성</h1>
    <p class="mt-2 text-lg text-gray-600">작성할 신청서의 카테고리를 선택하세요.</p>
  </div>

  <% if @categories.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @categories.each do |category| %>
        <%= link_to select_template_request_forms_path(category_id: category.id), 
                    class: "block bg-white rounded-lg shadow hover:shadow-lg transition-shadow duration-200 border border-gray-200 hover:border-indigo-500" do %>
          <div class="p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-2">
              <%= category.name %>
            </h3>
            <% if category.description.present? %>
              <p class="text-gray-600 text-sm">
                <%= category.description %>
              </p>
            <% end %>
            <div class="mt-4 flex items-center justify-between">
              <span class="text-sm text-gray-500">
                템플릿 <%= category.request_templates.active.count %>개
              </span>
              <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  <% else %>
    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-yellow-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M8.485 2.495c.673-1.167 2.357-1.167 3.03 0l6.28 10.875c.673 1.167-.17 2.625-1.516 2.625H3.72c-1.347 0-2.189-1.458-1.515-2.625L8.485 2.495zM10 5a.75.75 0 01.75.75v3.5a.75.75 0 01-1.5 0v-3.5A.75.75 0 0110 5zm0 9a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-yellow-800">
            사용 가능한 카테고리가 없습니다
          </h3>
          <div class="mt-2 text-sm text-yellow-700">
            관리자에게 문의하여 신청서 카테고리를 등록해 주세요.
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>