<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900">신청서 수정</h1>
    <p class="mt-2 text-lg text-gray-600"><%= @request_form.title %></p>
  </div>

  <%= render 'form', request_form: @request_form %>
</div>